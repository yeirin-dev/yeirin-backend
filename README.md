# Yeirin - 메인 백엔드

예이린 상담기관 매칭 플랫폼의 핵심 백엔드 서비스

## 🎯 주요 기능

### Phase 1 (현재 개발)
- ✅ 상담의뢰지 텍스트 수신
- ✅ AI MSA를 통한 상담기관 추천 요청
- ✅ 추천 결과 반환

### Phase 2 (예정)
- 회원 관리 (부모, 상담사, 상담기관)
- 상담사 승인 프로세스
- 상담 매칭 및 예약
- 면담결과지 관리

## 🏗️ 아키텍처

### DDD 기반 계층 구조
```
src/
├── domain/              # 순수 비즈니스 로직 (프레임워크 독립)
│   ├── matching/        # 매칭 도메인
│   │   ├── entity/
│   │   ├── value-object/
│   │   └── service/
│   └── shared/          # 공유 도메인 컴포넌트
│       ├── result.ts
│       └── base-entity.ts
│
├── application/         # 유스케이스 (비즈니스 흐름)
│   └── matching/
│       ├── request-counselor-recommendation.usecase.ts
│       └── dto/
│
├── infrastructure/      # 기술 구현체
│   ├── persistence/     # TypeORM 엔티티, 리포지토리
│   ├── external/        # 외부 API 클라이언트
│   │   └── ai-recommendation.client.ts
│   └── config/
│
└── presentation/        # API 계층
    └── http/
        └── matching/
            └── matching.controller.ts
```

## 🚀 시작하기

### 환경 설정
```bash
# 의존성 설치
yarn install

# 환경 변수 설정
cp .env.example .env

# 개발 서버 실행
yarn start:dev
```

### 테스트
```bash
# 단위 테스트
yarn test

# E2E 테스트
yarn test:e2e

# 커버리지
yarn test:cov
```

## 📝 개발 규칙

- **TDD 필수**: 테스트 먼저 작성
- **DDD 계층 준수**: Domain → Application → Infrastructure → Presentation
- **타입 안정성**: `any` 타입 절대 금지
- **네이밍**: 한글로 비즈니스 의미 명확히

자세한 내용은 [CLAUDE.md](../CLAUDE.md) 참고

## 🔗 API 문서

개발 서버 실행 후: http://localhost:3000/api

## 🛠️ 기술 스택

- **Framework**: NestJS 10
- **Language**: TypeScript 5
- **ORM**: TypeORM 0.3
- **Database**: PostgreSQL 15
- **Testing**: Jest
- **Validation**: class-validator
